<VirtualHost *:80>
    # Change the next line to your hostname
    ServerName 

    ProxyPreserveHost On

    ProxyPass 
    ProxyPassReverse 

    Header set Access-Control-Allow-Origin "*"
</VirtualHost>
